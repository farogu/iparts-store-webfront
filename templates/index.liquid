
<div class="hero">
  <div class="container">
    <h1>Repuestos iPhone de Calidad</h1>
    <p>Encuentra las mejores piezas para tu iPhone</p>
  </div>
</div>

{% render 'model-selector' %}

<div id="products-section">
  <div class="container">
    <h2>Productos Destacados</h2>
    <div id="shopify-featured-grid" class="product-grid">
      {% for product in collections.all.products limit: 8 %}
        {% render 'product-card', product: product %}
      {% endfor %}
    </div>
    
    <div id="sunsky-featured" class="product-grid" style="display: none;">
      <!-- Productos destacados de Sunsky -->
    </div>
  </div>
</div>

<div class="loader" style="display: none;">
  <div class="spinner"></div>
  <p>Cargando productos...</p>
</div>
